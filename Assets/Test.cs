using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {/*
        Vector3 v1 = GameObject.Find("Actor1").transform.position;
        Vector3 v2 = GameObject.Find("Actor2").transform.position;

        Vector3 displacementDifference = v1 - v2;
        displacementDifference.y += 2;
        this.transform.position = v2 + displacementDifference / 2;
        
*/

       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
